{
  "application": "bearing_fault_detection",
  "anticipated": {
    "name": "anticipated_scenario_1",
    "given": "h < 100 AND vibration >= 60",
    "when": "b > 5 AND dt > 0",
    "do":"action_x",
    "then": "dt_delta >= 5 AND h >= 100"
  },
  "unanticipated": [
    {
      "name": "unanticipated_scenario_1",
      "given": "h < 100 AND vibration >= 62",
      "when": "b > 8 AND dt > 0",
      "do":"action_x",
      "then": "dt_delta >= 8 AND h >= 100"
    }
  ],
  "diagnosed": [
    {
      "name": "diagnosed_scenario_1",
      "given": "h < 100 AND vibration >= 63",
      "when": "b > 10 AND dt > 0",
      "then": "dt_delta >= 10 AND h >= 100"
    }
  ],
  "candidate": [
    {
      "name": "candidate_scenario_1",
      "given": "h < 95 AND vibration >= 63",
      "when": "b > 12 AND dt > 0",
      "do":"action_x",
      "then": "dt_delta >= 12 AND h >= 110"
    }
  ],
  "adapted": [
    {
      "name": "adapted_scenario_1",
      "given": "h < 90 AND vibration >= 65",
      "when": "b > 15 AND dt > 0",
      "do":"action_x",
      "then": "dt_delta >= 15 AND h >= 120"
    }
  ]
}
