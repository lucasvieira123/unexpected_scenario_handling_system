{
  "expected": {
    "Takeoff": {
      "Given": "battery > 10",
      "When": "takeoff_initiated = 1",
      "Do": "takeoff",
      "Then": "altitude > 0 AND source_distance > 0"
    }
  },
  "unexpected": {
    "Landing": {
      "Given": "height > 0",
      "When": "distance_target <= 5",
      "Do": "landing",
      "Then": "height > 0 AND distance_target <= 5"
    }
  },
  "diagnosed": {
    "Landing": {
      "Given": "height > 0 AND obstacle != 0",
      "When": "distance_target <= 5",
      "Do": "landing",
      "Then": "height > 0 AND distance_target <= 5"
    }
  },
  "desired": [
    {
      "Takeoff": {
        "Given": "battery > 10 AND satellite_count <= 7",
        "When": "takeoff_initiated = 1",
        "Do": "unknown_action",
        "Then": "altitude > 0 AND source_distance > 0"
      }
    },
    {
      "Takeoff_with_strong_wind": {
        "Given": "battery > 10 AND wind_speed > 50",
        "When": "takeoff_initiated = 1",
        "Do": "unknown_action",
        "Then": "altitude > 0 AND source_distance > 0"
      }
    },
    {
      "Takeoff_with_gps_failures": {
        "Given": "battery > 10 AND gps_status == 0",
        "When": "takeoff_initiated = 1",
        "Do": "unknown_action",
        "Then": "altitude > 0 AND source_distance > 0"
      }
    },
    {
      "Takeoff_with_overweight": {
        "Given": "battery > 10 AND overweight = 1",
        "When": "takeoff_initiated = 1",
        "Do": "unknown_action",
        "Then": "altitude > 0 AND source_distance > 0"
      }
    }
  ]
}
